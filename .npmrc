# Polaris/polaris-viz declare React ^18; this app runs React 19 (works —
# smoke-tested per design doc §15). Vercel/npm must install past the peer
# conflict the same way local dev does.
legacy-peer-deps=true
