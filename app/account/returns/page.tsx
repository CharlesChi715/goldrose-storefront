/**
 * ROLE OF THIS FILE
 * /account/returns — Figma ACCOUNT-RETURNS-AFTER-SALES 1230:119, imported 2026-07-28.
 * Visual placeholder: the mock's own return cases; reached from the
 * dashboard's Returns & After-Sales row.
 */

import type { Metadata } from "next";
import { ReturnsScreen } from "@/components/screens/ReturnsScreen";

export const metadata: Metadata = {
  title: "Returns & after-sales — GoldRose",
  robots: { index: false },
};

export default function ReturnsPage() {
  return <ReturnsScreen />;
}
