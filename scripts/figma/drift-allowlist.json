{
  "_": "Deliberate design↔repo mismatches. figma:routes and figma:unbuilt skip these, so a settled decision is not re-examined every sync. Every entry needs a reason and a date.",
  "framesWithoutRoute": [
    {
      "route": "/account/returns/select-reason",
      "why": "Built as the ReturnReasonSheet bottom sheet, not a route — deliberate 2026-08-02 decision recorded in that component's header."
    }
  ],
  "routesWithoutFrame": [
    {
      "route": "/checkout/success",
      "why": "Order confirmation; technical outcome route the design never framed."
    },
    {
      "route": "/checkout/cancel",
      "why": "PayPal cancel return path; technical, no design."
    },
    {
      "route": "/policies/email-sms-terms",
      "why": "The frame exists (2127:238) but is named SCROLL-CONTENT instead of its route, so the matcher cannot pair them. It IS this page — titled \"Email & SMS Terms\", carrying the Policy G code, and imported from on 2026-08-18. Settled here rather than re-diagnosed every sync; drop this entry if the design team ever renames the frame per the route rule."
    }
  ]
}
